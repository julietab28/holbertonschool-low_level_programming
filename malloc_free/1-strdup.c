#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * _strdup - check
 * @str: array
 * Return: Always 0.
 */
char *_strdup(char *str)
{
	int a, b;
	char *str_2;

	if (str == NULL)
	{
		return (NULL);
	}
	
}
