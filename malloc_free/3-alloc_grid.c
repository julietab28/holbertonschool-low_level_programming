#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * **alloc_grid - 
 * @width: width of the grid
 * @height: height of the grid
 *
 * Return: Nothing.
 */
int **alloc_grid(int width, int height)
{
	int w, h;
	int *grid

	if (width <= 0)
	{
		return (NULL);
	}

	if (height <= 0)
	{
		return (NULL);
	}

	for (w = 0; w < width; w++)
	{
		grid[w] = 
	}
	for (h = 0; h < height; h++)
	{
	}

	
}
