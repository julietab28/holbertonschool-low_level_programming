#include <stdio.h>
#include "dog.h"
#include <stdlib.h>
/**
 * main - check the code
 *
 * Return: Always 0.
 */
dog_t *new_dog(char *name, float age, char *owner)
{
	if (name == NULL || owner == NULL)
	{
		
	}
	for (len1 = 0; name[len1] != '\0'; len1++)
	{
	}
}

