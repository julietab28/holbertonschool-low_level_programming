#include <stdio.h>

/**
 * main - la main mani
 *
 * Return: Always 0 (esitosamente)
 */
int main()
{
	char Frase[] = "Programming is like building a multilingual puzzle \n";
	
	puts(Frase);
	
	return (0);
}
