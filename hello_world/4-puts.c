#include <stdio.h>

/**
 * main - la main mani
 *
 * Return: Always 0 (esitosamente)
 */
int main(void)
{
	char Frase[] = "Programming is like building a multilingual puzzle";
	puts(Frase);
	return (0);
}
