#include "main.h"

/**
 * _puts - check the code
 * @str: lol
 * Return: Always 0.
 */
void _puts(char *str)
{
        int len;

        for (len = 0; str[len] != '\0'; len++)
        {
		_putchar(len);
        }
}
